//
// DO NOT EDIT THIS FILE, IT HAS BEEN GENERATED USING AndroidAnnotations.
//


package ru.zlo.ff.util;

import android.app.Activity;
import android.content.Context;
import android.content.res.Resources;
import android.view.View;
import ru.zlo.ff.R.array;
import ru.zlo.ff.R.drawable;

public final class Options_
    extends Options
{

    private Context context_;

    private Options_(Context context) {
        context_ = context;
        init_();
    }

    public void afterSetContentView_() {
        if (!(context_ instanceof Activity)) {
            return ;
        }
    }

    /**
     * You should check that context is an activity before calling this method
     * 
     */
    public View findViewById(int id) {
        Activity activity_ = ((Activity) context_);
        return activity_.findViewById(id);
    }

    @SuppressWarnings("all")
    private void init_() {
        if (context_ instanceof Activity) {
            Activity activity = ((Activity) context_);
        }
        Resources resources_ = context_.getResources();
        muss = resources_.getStringArray(array.muss);
        vids = resources_.getStringArray(array.vids);
        docs = resources_.getStringArray(array.docs);
        imgs = resources_.getStringArray(array.imgs);
        bin = resources_.getStringArray(array.bin);
        i_file_img = resources_.getDrawable(drawable.i_file_img);
        i_fold = resources_.getDrawable(drawable.i_fold);
        i_chk = resources_.getDrawable(drawable.i_chk);
        i_file_bin = resources_.getDrawable(drawable.i_file_bin);
        i_unchk = resources_.getDrawable(drawable.i_unchk);
        i_file_doc = resources_.getDrawable(drawable.i_file_doc);
        i_file_non = resources_.getDrawable(drawable.i_file_non);
        i_file_mus = resources_.getDrawable(drawable.i_file_mus);
        i_file_mov = resources_.getDrawable(drawable.i_file_mov);
        context = context_;
        loadOptions();
    }

    public static Options_ getInstance_(Context context) {
        return new Options_(context);
    }

    public void rebind(Context context) {
        context_ = context;
        init_();
    }

}
