//
// DO NOT EDIT THIS FILE, IT HAS BEEN GENERATED USING AndroidAnnotations.
//


package ru.zlo.ff.components.filerow;

import android.app.Activity;
import android.content.Context;
import android.view.View;
import android.view.View.OnClickListener;
import android.view.View.OnLongClickListener;
import android.widget.ImageView;
import android.widget.TextView;
import ru.zlo.ff.R.id;
import ru.zlo.ff.R.layout;


/**
 * We use @SuppressWarning here because our java code
 * generator doesn't know that there is no need
 * to import OnXXXListeners from View as we already
 * are in a View.
 * 
 */
@SuppressWarnings("unused")
public final class FileRow_
    extends FileRow
{

    private Context context_;
    private boolean mAlreadyInflated_ = false;

    public FileRow_(Context context, FileRowData data) {
        super(context, data);
        init_();
    }

    private void init_() {
        context_ = getContext();
        if (context_ instanceof Activity) {
            Activity activity = ((Activity) context_);
        }
    }

    private void afterSetContentView_() {
        m_icon = ((ImageView) findViewById(id.m_icon));
        m_txt_items = ((TextView) findViewById(id.m_txt_items));
        m_check = ((ImageView) findViewById(id.m_check));
        m_txt_dir = ((TextView) findViewById(id.m_txt_dir));
        m_txt_date = ((TextView) findViewById(id.m_txt_date));
        {
            View view = findViewById(id.m_icon);
            if (view!= null) {
                view.setOnClickListener(new OnClickListener() {


                    @Override
                    public void onClick(View view) {
                        FileRow_.this.iconClick(view);
                    }

                }
                );
            }
        }
        {
            View view = findViewById(id.m_check);
            if (view!= null) {
                view.setOnClickListener(new OnClickListener() {


                    @Override
                    public void onClick(View view) {
                        FileRow_.this.checkClick();
                    }

                }
                );
            }
        }
        {
            View view = findViewById(id.m_check);
            if (view!= null) {
                view.setOnLongClickListener(new OnLongClickListener() {


                    @Override
                    public boolean onLongClick(View view) {
                        return FileRow_.this.checkLongClick();
                    }

                }
                );
            }
        }
        initChildUI();
    }

    /**
     * The mAlreadyInflated_ hack is needed because of an Android bug
     * which leads to infinite calls of onFinishInflate()
     * when inflating a layout with a parent and using
     * the <merge /> tag.
     * 
     */
    @Override
    public void onFinishInflate() {
        if (!mAlreadyInflated_) {
            mAlreadyInflated_ = true;
            inflate(getContext(), layout.filerow, this);
            afterSetContentView_();
        }
        super.onFinishInflate();
    }

    public static FileRow build(Context context, FileRowData data) {
        FileRow_ instance = new FileRow_(context, data);
        instance.onFinishInflate();
        return instance;
    }

}
